🧰 第一步：准备工作 (Import Libraries)

# data analysis and wrangling
import pandas as pd
import numpy as np
import random as rnd

# visualization
import seaborn as sns
import matplotlib.pyplot as plt
%matplotlib inline

# machine learning
from sklearn.linear_model import LogisticRegression, Perceptron, SGDClassifier
from sklearn.svm import SVC, LinearSVC
from sklearn.ensemble import RandomForestClassifier
from sklearn.neighbors import KNeighborsClassifier
from sklearn.naive_bayes import GaussianNB
from sklearn.tree import DecisionTreeClassifier

这个代码块一次性导入了项目所需的所有库：

· 数据处理：pandas (负责数据读取和处理)、numpy (科学计算基础库)、random (生成随机值，用于填补缺失数据)。

· 数据可视化：seaborn 和 matplotlib 用于创建图表；%matplotlib inline 确保图表直接显示在 Notebook 中。

· 机器学习：从 scikit-learn 导入了多种分类算法，包括逻辑回归、支持向量机(SVC)、随机森林、K近邻(KNN)、朴素贝叶斯、感知机、随机梯度下降分类器和决策树。这为本项目后续的模型对比提供了多种选择。

📂 第二步：数据获取 (Acquire data)
python
train_df = pd.read_csv('../input/train.csv')
test_df = pd.read_csv('../input/test.csv')
combine = [train_df, test_df]
pandas 的 read_csv 函数负责将 CSV 文件加载到内存中，形成 DataFrame 对象。在此基础上，代码创建了一个名为 combine 的列表，这个设计非常巧妙，它使得后续的某些数据处理任务（如特征工程）可以在训练集和测试集上同步进行，保证了数据操作的一致性，是整个流程中的常用技巧。

🔍 第三步：探索性数据分析 (Exploratory Data Analysis - EDA)
这一步的目标是建立对数据的直观理解。

1. 初步了解数据：

· print(train_df.columns.values)：打印所有列名，让我们知道数据集中包含哪些特征。

· train_df.head()：查看前5行数据，快速了解每个特征的大致内容和格式。

· train_df.tail()：查看后5行数据，有助于发现数据末尾是否有异常或特殊值。

· train_df.info()：这是一个至关重要的命令，它能清晰地展示每个特征的非空值数量和数据类型，从而帮助我们快速定位到需要处理的缺失值。例如，从输出中可以看出，Age (年龄) 特征在训练集中有714个非空值，意味着有177个缺失值。

· train_df.describe()：展示数值型特征的描述性统计信息，如总数、均值、标准差、四分位数和最大/最小值。例如，它显示了约38%的乘客幸存，并且大多数乘客（>75%）没有与父母或孩子同行。

· train_df.describe(include=['O'])：专门用于查看对象（文本）类型特征的统计信息，例如 Name 的唯一值数量为891，说明名字都是唯一的。

2. 分析特征与生存率的关系：

· 示例代码 train_df[['Pclass', 'Survived']].groupby(['Pclass'], as_index=False).mean().sort_values(by='Survived', ascending=False)：

  · 它会根据船舱等级 (Pclass) 对数据进行分组。然后计算每个舱位等级下的平均生存率。

  · 结果显示，头等舱（Pclass=1）乘客的生存率约为63%，而三等舱（Pclass=3）的生存率仅为24%。

· 同样的分析方法也适用于 Sex（性别，女性生存率74% vs 男性19%），SibSp（兄弟姐妹/配偶数），Parch（父母/子女数）。

3. 数据可视化：

· 连续变量相关性：sns.FacetGrid(train_df, col='Survived').map(plt.hist, 'Age', bins=20)。这行代码创建了一个按 Survived 分列的面板网格图，并分别绘制了幸存者和遇难者的年龄分布直方图，直观展示了年龄与生存率的关系。

· 多变量相关性：sns.FacetGrid(train_df, col='Survived', row='Pclass', size=2.2, aspect=1.6).map(plt.hist, 'Age', alpha=.5, bins=20)。这个图按船舱等级 (Pclass) 分行、按是否幸存 (Survived) 分列，绘制了年龄分布，使我们可以观察不同舱位和生存状态下的年龄分布差异。

· 类别变量相关性：sns.FacetGrid(train_df, row='Embarked', size=2.2, aspect=1.6).map(sns.pointplot, 'Pclass', 'Survived', 'Sex', palette='deep')。该代码生成了一个按登船港口 (Embarked) 分行的面板图，在每个子图中使用点图展示了不同舱位 (Pclass) 和性别 (Sex) 组合下的生存率，可以用来探究不同港口的乘客生存率是否存在差异。

🛠️ 第四步：数据清洗与特征工程 (Data Wrangling & Feature Engineering)
在分析之后，开始对数据进行处理，使其适合模型输入。

1. 丢弃无用特征：

python
train_df = train_df.drop(['Ticket', 'Cabin'], axis=1)
test_df = test_df.drop(['Ticket', 'Cabin'], axis=1)
combine = [train_df, test_df]
这一步直接删除了 Ticket（票号）和 Cabin（客舱号）特征。Cabin 缺失值太多，难以有效利用；Ticket 则是作者基于经验判断，认为其与生存率的关联不大。

2. 从姓名中提取称谓 (Title)：

python
for dataset in combine:
    dataset['Title'] = dataset.Name.str.extract(' ([A-Za-z]+)\.', expand=False)
这是一个非常经典的特征工程操作。使用正则表达式 ' ([A-Za-z]+)\.' 从 Name 列中提取出称谓（如 Mr, Mrs, Miss, Master 等）。之后，通过 replace 方法将许多罕见的称谓归类为 'Rare'，并将法语称谓 'Mlle', 'Mme' 等映射到对应的英文称谓，从而简化了分类。

3. 转换类别特征为数值：

python
for dataset in combine:
    dataset['Sex'] = dataset['Sex'].map( {'female': 1, 'male': 0} ).astype(int)
机器学习模型只能处理数值，因此需要将文字类别转换为数字。这里通过 .map() 函数将 female 映射为 1，male 映射为 0。同理，Embarked（登船港口）也会通过映射转换为 0，1，2；而 Title 特征则先被转换为数字（如 Mr:1, Miss:2...），最终也作为数值特征参与建模。

4. 填补缺失值：

· 年龄 (Age) 填补：年龄包含缺失值。笔记本代码使用了一种精细的填补策略：它根据乘客的性别 (Sex) 和船舱等级 (Pclass) 分组，计算每个组里年龄的中位数，然后用这个中位数来填充该组内缺失的年龄值。这种方法比简单地用全局平均值填补更加准确。

· 登船港口 (Embarked) 填补：该特征缺失值很少。通过 freq_port = train_df.Embarked.dropna().mode()[0] 找到出现次数最多的港口（众数，即 'S'）进行填充。

· 票价 (Fare) 填补：测试集中 Fare 有一个缺失值。笔记本使用 test_df['Fare'].fillna(test_df['Fare'].dropna().median(), inplace=True)，利用测试集票价的中位数来填充。

5. 创建新特征：

python
for dataset in combine:
    dataset['FamilySize'] = dataset['SibSp'] + dataset['Parch'] + 1
这是另一个特征工程的经典示例。我们将 SibSp（兄弟姐妹/配偶数）和 Parch（父母/子女数）两个特征相加，再加上乘客自身，得到一个新的特征 FamilySize（家庭总人数）。

6. 分箱 (Binning) 处理：

python
for dataset in combine:
    dataset['AgeBand'] = pd.cut(dataset['Age'], 5)
将连续的年龄值切分成几个年龄段（例如儿童、青年、中年、老年等），再将这些年龄段转换为数值标签。这样做可以简化模型，并有助于发现非线性关系。

🤖 第五步：建模、预测与评估 (Modeling, Predicting and Evaluation)
这是最后也是最核心的环节，将准备好的数据用于训练和评估模型。

1. 数据准备：首先，使用 X_train = train_df.drop("Survived", axis=1) 将特征数据与目标变量 Survived 分离。

2. 模型训练：针对每种选定的模型（如逻辑回归、SVM、随机森林等）：

· 创建一个模型实例：model = RandomForestClassifier(n_estimators=100)

· 使用训练数据训练模型：model.fit(X_train, Y_train)

· 对测试集进行预测：prediction = model.predict(X_test)

3. 模型评估：使用 accuracy_score(Y_pred, Y_true) 等指标来评估各个模型在验证集上的表现。

4. 结果提交：最后，将最优模型的预测结果按照 Kaggle 要求的格式保存为 CSV 文件，用于提交并获取评分。

总的来说，这个项目的核心价值在于它的思路：每一步对数据的观察、每一个特征被留下或被剔除，背后都有明确的理由和实验数据的支撑。理解这个“提出问题—观察数据—做出假设—动手验证”的循环，是入门数据科学的关键。

对于模型评估，
acc_xxx 分数，实际上就是 准确率（Accuracy），它是分类问题中最基础的评估指标之一。

准确率的定义
准确率 = 预测正确的样本数 / 总样本数

例如，如果模型在 100 个测试乘客中正确预测了 85 个人的生存状态（幸存或遇难），那么准确率就是 85%（0.85）。

这些 acc 分数具体是怎么来的？

1. 划分数据：把 train_df 分成训练集和验证集（例如用 train_test_split），或者直接在整个训练集上训练，然后在测试集（test_df）上预测并与真实值对比（Titanic 竞赛中测试集没有真实标签，所以更常见的是使用交叉验证或从训练集再分出一个验证集）。

2. 训练模型：对每个算法（SVM、KNN、逻辑回归等），用训练数据拟合模型。

3. 预测与评估：用模型预测验证集，再用 accuracy_score(y_true, y_pred) 计算出准确率，存入变量 acc_svc、acc_knn 等。

因此，最终表格里的 Score 列就是 各个模型在验证集上预测乘客是否幸存时，回答正确的比例。

为什么分数有高有低？
不同机器学习算法对 Titanic 数据的“拟合能力”和“泛化能力”不同：

· 随机森林（Random Forest） 通常得分较高（比如 0.8 以上），因为它能捕捉特征间的复杂交互，且不易过拟合。

· 朴素贝叶斯（Naive Bayes） 得分可能低一些，因为它假设特征独立，这在真实数据中往往不成立。

· 感知机（Perceptron） 是线性模型，处理非线性关系能力弱，分数可能不高。

作者通过这张表格快速比较哪种模型在当前数据上表现最好，然后通常会把 最高分的模型 拿去做最终的测试集预测并提交到 Kaggle。

注意：这里的准确率只是初步评估。更严谨的做法还需要看精确率、召回率、F1 分数，或者做交叉验证。但对 Titanic 入门项目来说，准确率已经足够用来选一个“够好”的模型。

另外，过拟合是指模型在训练集上表现极好，但在测试集（或新数据）上表现很差，即模型“记住”了训练数据中的噪声和细节，而没有学到真正的规律。避免过拟合的核心思想是：限制模型复杂度 或 增加训练数据的多样性。

以下是几种常用且有效的防过拟合方法，按实践中的常见程度排序：

1️⃣ 增加训练数据量（最根本）
· 数据越多，模型越难“背”下所有样本，只能学到通用模式。

· 方法：收集更多真实数据、数据增强（如图像旋转/裁剪、文本同义词替换等）。

2️⃣ 降低模型复杂度
· 模型太复杂（层数多、神经元多、特征多）容易过拟合。适当简化：

  · 减少神经网络层数/神经元数量。

  · 决策树中限制最大深度（max_depth）、增加叶节点最少样本数（min_samples_leaf）。

  · 减少特征数量（特征选择，比如只保留重要性高的特征）。

3️⃣ 正则化（Regularization）
· 在损失函数中加入惩罚项，限制模型参数的大小。

  · L1 正则化（Lasso）：使部分参数变为 0，产生稀疏解。

  · L2 正则化（Ridge / weight decay）：使参数整体变小但不为零。

· 常见库中的参数：C（逻辑回归/SVM，C 越小正则化越强），alpha（线性模型），penalty 参数。

4️⃣ 早停（Early Stopping）
· 在训练过程中同时监控验证集误差，当验证集误差不再下降（甚至上升）时，提前终止训练。

· 适用于神经网络、梯度提升树等迭代式算法。

5️⃣ 交叉验证（Cross-Validation）
· 不固定训练/验证集划分，而是使用 k 折交叉验证评估模型稳定性。虽然不能直接减少过拟合，但可以帮助你发现过拟合，并调整超参数。

6️⃣ 集成学习（Ensemble）
· 组合多个弱模型来降低方差。

  · Bagging（如随机森林）：对数据随机采样训练多棵树，取平均。

  · Boosting（如 XGBoost）：逐步修正前一个模型的错误，同时加入学习率等防止过拟合。

· 集成方法本身就有很强的抗过拟合能力。

7️⃣ Dropout（专用于神经网络）
· 训练时随机“丢弃”一部分神经元，迫使网络不依赖特定特征，学得更鲁棒。

8️⃣ 降低训练轮数（Epochs）或增加学习率
· 简单神经网络训练太久会过拟合，适当减少 epoch 或使用更大的学习率（配合学习率衰减）有时有帮助。

9️⃣ 噪声注入
· 在输入数据或标签中加入少量随机噪声，迫使模型学习更平滑的函数。

在 Titanic 项目中的具体应用（供参考）
方法	            如何在本项目中实现
降低模型复杂度	    使用决策树时设置 max_depth=3 或 min_samples_split=10；逻辑回归使用 C=0.1（增强正则化）
特征选择	          只保留 Pclass, Sex, Age, Fare, Title 等最相关的特征，丢弃 Ticket, Cabin 等噪声特征（笔记本中已做）
交叉验证	          用 cross_val_score(RandomForestClassifier(), X_train, y_train, cv=5) 查看平均准确率
集成模型	          随机森林本身就是 Bagging 集成，比单棵决策树更不容易过拟合
早停	            如果训练神经网络，可以用 EarlyStopping 回调

最后记住一个判断过拟合的简单方法：训练集准确率远高于验证集准确率（例如训练集 99%，验证集 80%），说明模型过拟合了。此时就需要应用上述方法之一或多个。

要达到这些调整完全可以基于现成的 scikit-learn 模型，通过调整参数（超参数） 和使用交叉验证等工具 来有效避免过拟合，完全不需要自己写底层算法函数。

具体怎么做？（全部使用 sklearn 现有功能）
1️⃣ 调整模型参数（超参数调优）
每个 sklearn 模型都提供了控制复杂度的参数，你只需要像设置 n_neighbors=3 一样，修改这些参数即可：

模型	    防过拟合的关键参数	                                              示例
逻辑回归	  C（正则化强度，越小正则化越强）	                                  LogisticRegression(C=0.1)
决策树	    max_depth（最大深度）, min_samples_split（最小分裂样本数）	        DecisionTreeClassifier(max_depth=3, min_samples_split=10)
随机森林	  max_depth, min_samples_leaf,                                    n_estimators	RandomForestClassifier(n_estimators=100, max_depth=5)
SVM	      C（惩罚系数）, gamma（RBF核参数）	                                SVC(C=0.8, gamma='scale')
KNN	      n_neighbors（增大 k 值使边界更平滑）	                            KNeighborsClassifier(n_neighbors=7)

你不需要自己写正则化公式或剪枝算法，sklearn 内部已经实现好了。

2️⃣ 使用交叉验证评估（而非只看训练集分数）
sklearn 提供 cross_val_score，可以自动帮你做 K 折验证，评估模型的泛化能力：

python
from sklearn.model_selection import cross_val_score
scores = cross_val_score(LogisticRegression(C=0.1), X_train, y_train, cv=5)
print(scores.mean())
这也不需要自己写循环划分数据的函数。

3️⃣ 使用网格搜索自动找最佳参数（同时防过拟合）
GridSearchCV 结合交叉验证，自动搜索最佳的超参数组合，避免手动试错：

python
from sklearn.model_selection import GridSearchCV
param_grid = {'C': [0.01, 0.1, 1, 10], 'max_depth': [3, 5, None]}
grid = GridSearchCV(DecisionTreeClassifier(), param_grid, cv=5)
grid.fit(X_train, y_train)
print(grid.best_params_)
4️⃣ 早停（针对梯度提升等迭代模型）
sklearn 中的 SGDClassifier、HistGradientBoostingClassifier 等支持 early_stopping 参数，设置后会自动在验证集性能不再提升时停止训练：

python
from sklearn.linear_model import SGDClassifier
sgd = SGDClassifier(early_stopping=True, validation_fraction=0.1)








