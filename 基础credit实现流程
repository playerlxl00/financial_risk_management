参考：
https://www.kaggle.com/code/nicholasgah/eda-credit-scoring-top-100-on-leaderboard
https://www.kaggle.com/code/willkoehrsen/start-here-a-gentle-introduction

📘 Part 1: 通用 EDA 框架
EDA 是整个数据分析的起点，其核心目标是理解数据、发现模式、识别问题。Will 采用了高度结构化的 EDA 通用框架，其思路在多个项目中具有普适性。

1. 数据概览 (Data Overview)
方法	                        目的	                                               输出
df.head() / df.sample()	      查看数据样本，理解特征含义和格式	                       前几行/随机样本数据
df.info()	                    获取数据类型、非空值数量、内存使用情况	                 每列的数据类型和缺失情况
df.describe()	                查看数值型特征的统计摘要（均值、标准差、分位数等）	       统计指标表
df.isnull().sum()	            统计各列的缺失值数量	                                 缺失值计数
df.dtypes.value_counts()	    了解数据类型分布	                                     各类数据类型的列数
融合建议：

· 将这份通用 EDA 模板作为信用评分项目的固定第一步，快速建立对数据的全局认知。

· 与 Nicholas 的 Notebook 的深度 EDA 互补：Will 的流程侧重快速概览，Nicholas 的 Notebook 更进一步，深入分析特征与目标变量之间的关系、多重共线性等复杂问题。

2. 缺失值处理 (Missing Value Handling)
Will 的 Notebook 提供了系统的缺失值处理流程，强调依赖模式和分布分析：

· 依赖分析：检查缺失值是否与目标变量或其他特征相关。例如，如果某个高缺失率特征与目标变量强相关，可能包含重要信息，不能直接删除。

· 分布分析：比较有缺失和无缺失样本在其他特征上的分布。如果分布相似，说明缺失可能是随机的（MCAR），可以使用均值/中位数填充；如果分布显著不同，则缺失可能与某些特定模式相关（MAR），需要更谨慎处理。

· 可视化辅助：使用 missingno 库的 matrix 和 bar 图表，直观查看缺失值的分布模式和比例。

融合建议：

· 将依赖分析和分布分析引入 Nicholas 的流程，特别是对于缺失率在 20%-50% 之间的特征，这种分析能帮助决定填充方案或是否保留。

· 在最终报告中增加缺失值可视化章节（使用 missingno），直观展示缺失模式。

3. 目标变量分析 (Target Variable Analysis)
Will 的 Notebook 对目标变量 TARGET 的分布分析系统而深入：

· 分布可视化：使用 sns.countplot() 检查类别平衡性。在信用评分中，负样本（违约）通常占比很低，类别不平衡是模型面临的巨大挑战。

· 时间维度分析：通过 TARGET 与时间相关变量（如 DAYS_BIRTH）的交叉分析，发现业务模式。例如，年轻客户可能违约率更高，这是后续特征工程的重要方向。

· 目标编码：将目标变量的均值作为新特征的编码方式，用于处理高基数分类变量，将类别信息转化为与目标相关的数值。

融合建议：

· 在信用评分项目中，花时间对目标变量进行深入分析是发现关键业务模式的重要途径，能直接指导特征工程的方向。

· 保留 Nicholas 的 WOE/IV 分析作为核心评估指标，补充 Will 的目标编码方法作为备选方案。Will 的 Notebook 还提供了另一个入门视角——从业务误判代价出发：金融场景中，漏掉一个真实违约者（假阴性）远比错判一个好人（假阳性）代价更高，因此在评估模型时需要更关注召回率而非单纯准确率。

4. 特征相关性分析 (Feature Correlation Analysis)
Will 的 Notebook 提供系统性的相关性分析流程：

· 流程对比：

  · Will 的 Notebook：首先使用 df.corr() 计算所有数值特征之间的相关系数；然后使用 sns.heatmap() 绘制相关性矩阵热图，快速识别高相关性特征对。

  · Nicholas 的 Notebook：更聚焦于特征与目标变量的关系，通过 IV 值筛选预测能力强的特征。

· 高相关性处理策略：

  · 保留与目标变量相关性更强的：如果两个特征高度相关（相关系数 > 0.7），保留与目标变量相关性更强的。

  · 结合业务理解：考虑特征的业务含义，选择更具解释性的保留。

  · 使用正则化模型：对于线性模型（如逻辑回归），可以使用 L1（Lasso）或 L2（Ridge）正则化自动处理多重共线性问题。

融合建议：

· 将 Will 的全局相关性分析与 Nicholas 的目标变量相关性分析结合：首先使用 Will 的流程识别高相关性特征对，然后使用 Nicholas 的 IV 值决定保留哪个特征。

· 在特征筛选阶段，将 Nicholas 的 IV 值阈值与 Will 的相关系数阈值结合使用，构建更稳健的特征筛选标准。

⚙️ Part 2: 深度特征工程教学
Will Koehrsen 的 Notebook 在特征工程方面提供了极其详尽的教学方法和创新思路，这是与 Nicholas 的精炼竞赛方案最显著的区别。

1. 创建新特征 (Create New Features)
Will 展示了多种从现有特征组合出全新特征的方法，这对提升模型效果至关重要。

信用评分领域的典型应用：在信用评分场景下，可以基于现有特征创造出有业务含义的新特征，例如：

· 比值类特征：总负债/总收入（负债收入比），是评估还款能力的关键指标。

· 差值类特征：贷款申请年限 - 年龄，反映申请人的贷款经验。

· 交互类特征：收入 × 信用额度，表征潜在的资金运用能力。

· 聚合类特征：对历史行为数据的统计值（如过去 6 个月的平均逾期天数）。

融合建议：

· Will 的特征创建方法与 Nicholas 的 WOE 编码是互补的：Will 的方法生成原始特征组合，Nicholas 的方法将这些特征转化为适合逻辑回归的形式。

· 在特征工程阶段，先使用 Will 的方法创建 10-20 个候选特征，然后通过 Nicholas 的 WOE/IV 流程筛选出预测能力最强的特征子集。

2. 分组聚合 (Group by Aggregates)
针对 Home Credit 数据集中多表关联的结构，Will 展示了如何使用 groupby 对关联表的记录进行聚合操作，生成客户级别的统计特征。常用聚合方法包括 mean()、sum()、min()、max()、count()、nunique()、std() 等。

信用评分领域的典型应用：

· 历史贷款记录：对客户的过往贷款记录进行聚合，例如 平均逾期天数、总贷款金额、贷款次数、贷款类型数 等。

· 信用卡消费记录：对客户的交易记录进行聚合，例如 月均消费金额、最大单笔消费、消费类别数 等。

· 信用查询记录：对机构的查询次数进行聚合，例如 过去 3 个月查询次数、不同查询机构数 等。

融合建议：

· Nicholas 的 Notebook 主要依赖单一表格的特征，Will 的多表聚合方法是重要的补充，能生成更多样化的特征，提升模型的预测能力。

· 在信用评分项目中，应深入挖掘数据的所有关联表（如还款记录、消费记录、查询记录等），通过聚合操作生成大量候选特征。

3. 特征选择流程 (Feature Selection Pipeline)
Will 的 Notebook 提供了完整的特征选择流程，与 Nicholas 的方法互补：

· 方法对比：

  · Will 的方法：基于相关性分析（排除高相关特征）、基于缺失率（排除缺失过多的特征）、基于特征重要性（使用随机森林筛选）。

  · Nicholas 的方法：基于 IV 值（排除 IV < 0.02 的弱预测特征）、基于 WOE 单调性（排除不符合业务逻辑的特征）、基于模型系数稳定性（使用交叉验证验证）。

· 融合策略：

  · 第一步：使用 Will 的方法快速过滤明显的无效特征（缺失率 > 80%、与目标变量相关性几乎为零）。

  · 第二步：对剩余特征计算 IV 值，根据 IV 值进一步筛选。

  · 第三步：对通过筛选的特征进行 WOE 转换，检查单调性是否符业务逻辑。

📊 Part 3: 可视化驱动洞察
Will 的 Notebook 使用丰富多样的可视化图表揭示数据背后的模式和业务规律，这是 Nicholas 的精炼方案中相对简化的部分。

1. 目标变量分布可视化
· 分析方法：使用 sns.countplot() 直观展示目标变量的类别分布，标注出正负样本的具体比例。

· 业务洞察：在信用评分中，负样本（违约）占比通常很低（如 2%-10%），这是一个高度不平衡的分类问题。

2. 特征与目标关系可视化
· 连续特征：

  · 使用 sns.kdeplot() 分别绘制违约/正常客户的概率密度曲线，对比两组分布的差异。

  · 使用 sns.boxplot() 对比违约/正常客户的四分位数范围，观察特征分布的差异。

  · 使用 sns.violinplot() 更精细地展示分布形态。

· 离散特征：

  · 使用 sns.barplot() 展示不同类别的违约率，发现高风险客户群体的特征模式。

  · 使用 sns.countplot() 对比违约/正常客户在不同类别上的数量分布。

3. 特征重要性可视化
· 分析方法：训练一个简单的随机森林模型，提取 model.feature_importances_，使用 sns.barplot() 按重要性排序展示 Top N 特征。

· 业务价值：通过特征重要性排序，可以快速识别哪些特征对预测贡献最大，指导特征工程的迭代方向。同时还能发现与业务直觉相符/相悖的结果，提示可能的数据问题。

融合建议：

· 将 Will 的可视化分析方法与 Nicholas 的数值分析结果（如 IV 值、WOE 单调性）结合，用图表增强分析报告的可读性和说服力。

· 在 EDA 报告中，使用 Will 的特征重要性条形图作为特征筛选的可视化依据，让非技术人员也能理解关键特征的预测作用。

· 可视化表格：Will 在 Notebook 中大量使用表格来展示数据，例如比较违约/正常客户的均值差异，这种清晰的数据表格同样值得在最终报告中保留。

🧠 Part 4: 模型训练与评估基础
Will 的 Notebook 在模型部分提供了基础但全面的教学内容，与 Nicholas 的进阶方法形成层次互补。

特征重要性分析 (Feature Importance)
核心思路：训练一个简单的随机森林模型，通过 model.feature_importances_ 获取每个特征的重要性分数，并可视化展示。这一步的核心目标不是追求最高预测精度，而是：

1. 验证特征工程效果：确认新创建的特征是否确实对预测有贡献。

2. 指导特征筛选：快速识别并剔除重要性极低的特征。

3. 发现业务规律：重要性高的特征往往对应了关键的客户行为模式，这些发现可以直接用于业务决策。

融合建议：

· 将特征重要性分析作为 Nicholas 的进阶模型流程的中间验证步骤。在完成特征工程后，先用随机森林验证新特征的有效性，再进入 WOE/IV 和评分卡构建。

· 在信用评分项目中，特征重要性分析能帮助筛选出有价值的候选特征，节省后续 WOE/IV 计算的时间。

🤖 Part 5: 自动特征工程简介
Will 在 Notebook 中简要介绍了 Featuretools 库，展示了如何利用自动化工具大幅提升特征构建效率。这是与"手动特征工程"完全不同的思路。

Featuretools 工作原理
1. 定义实体 (Entities)：将每个数据表定义为一个实体，指定其索引列。

2. 定义关系 (Relationships)：通过外键关联不同实体，形成数据关系图。

3. 生成特征 (Deep Feature Synthesis)：自动沿关系路径生成特征，例如：

  · 聚合特征：对关联表的记录进行 sum、mean、max、min、count 等操作。

  · 变换特征：对单个实体的特征进行 multiply、subtract 等运算。

融合建议：

在信用评分项目中，将 Will 的手动特征工程与 Featuretools 自动特征工程结合使用：

第一步：使用 Featuretools 快速生成大量候选特征（可达数百个）。

第二步：使用 Nicholas 的 WOE/IV 流程筛选出最有预测力的特征子集。

第三步：对于筛选出的关键特征，使用 Will 的手动方法精细调整其分箱和转换方式。

🔗 融合方案总结
两份 Notebook 的核心差异与融合互补关系如下：

分析维度	      Nicholas Gah (竞赛优化)	   Will Koehrsen (教学指南)	       融合策略
EDA 框架	    精炼，直接聚焦关键特征	     通用流程，系统全面	               Will 的流程作为通用模板，Nicholas 的分析作为深度补充
缺失值处理	    简单填充或删除	             依赖分析和分布分析	               将 Will 的依赖分析引入，为填充方案提供依据
目标变量分析	  简单分布检查	               时间维度、业务模式挖掘	           使用 Will 的方法发现业务规律，指导特征工程方向
相关性分析	    聚焦目标变量相关性	         全局特征间相关性	               先做全局识别高相关特征对，再根据 IV 值筛选
特征工程	      WOE/IV 转换	               多表聚合、特征组合、自动生成	     用 Will 的方法生成候选特征，用 Nicholas 的方法筛选和转换
特征筛选	      基于 IV 值阈值	             基于相关性和缺失率	               多阶段筛选：先用 Will 的方法快速过滤，再用 Nicholas 的方法精确筛选
可视化	        偏向简洁展示	               丰富多样，深度洞察	               使用 Will 的可视化方法增强分析报告的可读性
模型评估	      评分卡、Stacking	           特征重要性分析	                 特征重要性分析作为特征工程的验证步骤，评分卡作为最终输出

实践融合流程建议
1. 数据理解阶段：采用 Will 的通用 EDA 模板快速建立全局认知。

2. 数据清洗阶段：结合 Will 的依赖分析确定缺失值处理策略。

3. 特征工程阶段：

· 使用 Will 的多表聚合方法生成基础候选特征。

· 使用 Featuretools 自动生成更多候选特征。

· 使用 Nicholas 的 WOE/IV 流程筛选和转换特征。

4. 模型构建阶段：

· 使用 Will 的特征重要性分析验证特征有效性。

· 使用 Nicholas 的评分卡构建和 Stacking 追求最优预测性能。
